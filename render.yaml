services:
  # ===========================================
  # Main Web Application
  # ===========================================
  - type: web
    name: alharamain-store
    runtime: docker
    dockerfilePath: ./docker/php/Dockerfile
    dockerContext: .
    plan: starter
    region: frankfurt
    healthCheckPath: /health
    envVars:
      # Application
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_URL
        value: ""
      - key: APP_KEY
        generateValue: true
      - key: APP_TIMEZONE
        value: UTC
      - key: APP_LOCALE
        value: en
      
      # Database - Set these manually with your PlanetScale credentials
      - key: DB_CONNECTION
        value: mysql
      - key: DB_HOST
        value: ""
      - key: DB_PORT
        value: "3306"
      - key: DB_DATABASE
        value: ""
      - key: DB_USERNAME
        value: ""
      - key: DB_PASSWORD
        value: ""
      - key: MYSQL_ATTR_SSL_CA
        value: /etc/ssl/certs/ca-certificates.crt
      
      # Redis - References the Render Key Value service
      - key: REDIS_URL
        fromService:
          name: alharamain-cache
          type: keyvalue
          property: connectionString
      - key: REDIS_HOST
        fromService:
          name: alharamain-cache
          type: keyvalue
          property: host
      - key: REDIS_PORT
        fromService:
          name: alharamain-cache
          type: keyvalue
          property: port
      
      # Cache & Session
      - key: CACHE_STORE
        value: redis
      - key: SESSION_DRIVER
        value: redis
      - key: SESSION_LIFETIME
        value: "120"
      - key: QUEUE_CONNECTION
        value: redis
      
      # Logging
      - key: LOG_CHANNEL
        value: stderr
      - key: LOG_LEVEL
        value: warning
      
      # Mail
      - key: MAIL_MAILER
        value: log
      
      # Auto Migration
      - key: AUTO_MIGRATE
        value: "true"

  # ===========================================
  # Background Worker (Queue Processing)
  # ===========================================
  - type: worker
    name: alharamain-worker
    runtime: docker
    dockerfilePath: ./docker/php/Dockerfile
    dockerContext: .
    plan: starter
    region: frankfurt
    dockerCommand: php artisan queue:work redis --sleep=3 --tries=3 --max-time=3600
    envVars:
      # Application
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_KEY
        fromService:
          name: alharamain-store
          type: web
          envVarKey: APP_KEY
      
      # Database - Same as web service
      - key: DB_CONNECTION
        value: mysql
      - key: DB_HOST
        value: ""
      - key: DB_PORT
        value: "3306"
      - key: DB_DATABASE
        value: ""
      - key: DB_USERNAME
        value: ""
      - key: DB_PASSWORD
        value: ""
      - key: MYSQL_ATTR_SSL_CA
        value: /etc/ssl/certs/ca-certificates.crt
      
      # Redis
      - key: REDIS_URL
        fromService:
          name: alharamain-cache
          type: keyvalue
          property: connectionString
      - key: REDIS_HOST
        fromService:
          name: alharamain-cache
          type: keyvalue
          property: host
      - key: REDIS_PORT
        fromService:
          name: alharamain-cache
          type: keyvalue
          property: port
      - key: QUEUE_CONNECTION
        value: redis

  # ===========================================
  # Redis/Key Value Cache & Queue (Managed by Render)
  # Note: 'redis' type is deprecated, use 'keyvalue' instead
  # ===========================================
  - type: keyvalue
    name: alharamain-cache
    ipAllowList: []  # Only allow internal connections from Render services
    region: frankfurt
    plan: starter
    maxmemoryPolicy: allkeys-lru
